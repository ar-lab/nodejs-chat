{
  "name": "chat",
  "version": "0.0.1",
  "description": "Chat Application",
  "main": "server.js",
  "dependencies": {
    "express": "^4.15.4",
    "socket.io": "^2.0.3"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
